services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8290:8290"

  dashboard:
    image: nginx:1.27-alpine
    depends_on:
      - api
    ports:
      - "8080:80"
    volumes:
      - ./dashboard:/usr/share/nginx/html:ro
